import AdminDispensaryManager from '@/components/AdminDispensaryManager';

export const metadata = {
  title: 'GeoWeedo Admin · Dispensaries',
};

export default function AdminDispensariesPage() {
  return <AdminDispensaryManager />;
}
