'use client';
import { useEffect,useState } from 'react';import styles from '../admin.module.css';
export default function AdminWalletPage(){const[data,setData]=useState<any>(null),[error,setError]=useState(''),[copied,setCopied]=useState(''),[selectedWallet,setSelectedWallet]=useState(''),[busy,setBusy]=useState(false),[notice,setNotice]=useState('');async function load(){const r=await fetch('/api/admin/wallet',{cache:'no-store'});if(r.status===401){location.href='/admin/login';return;}const d=await r.json();if(!r.ok)throw new Error(d.error||'Failed to load wallet dashboard');setData(d);if(!selectedWallet&&d.walletUsers?.length)setSelectedWallet(d.walletUsers[0].walletId);}useEffect(()=>{load().catch(e=>setError(e.message));},[]);const fmt=(v:any)=>v?new Date(v).toLocaleString():'—';async function copyAddress(address:string){try{await navigator.clipboard.writeText(address);setCopied(address);setTimeout(()=>setCopied(''),1500);}catch{}}async function assignAddress(){if(!selectedWallet)return;setBusy(true);setNotice('');try{const r=await fetch('/api/admin/wallet',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({walletId:selectedWallet})}),d=await r.json();if(!r.ok)throw new Error(d.error||'Could not assign deposit address.');setNotice(d.existing?`Existing deposit address assigned: ${d.address}`:`New deposit address created: ${d.address}`);await load();}catch(e){setNotice(e instanceof Error?e.message:'Could not assign deposit address.');}finally{setBusy(false);}}
return <main className={styles.shell}><header className={styles.header}><div><a href="/admin" className={styles.adminHomeLink}><span className={styles.eyebrow}>GEOWEEDO ADMIN</span></a><h1>Yerbas wallet dashboard</h1><p>Unified custody view for treasury and player wallets, deposit addresses, ledger balance, rewards, deposits, and withdrawals.</p></div><div className={styles.headerActions}><a className={styles.ghost} href="/admin">Control center</a><a className={styles.ghost} href="/admin/users">User information</a></div></header>{error?<section className={styles.section}><p>{error}</p></section>:!data?<div className={styles.loading}>Loading wallet dashboard…</div>:<><section className={styles.section}><article className={styles.card} style={{minHeight:0,marginBottom:14,borderColor:'rgba(103,214,110,.35)'}}><span className={styles.eyebrow}>TREASURY WALLET ADDRESS</span><h3>GeoWeedo Treasury</h3>{data.rpc?.treasuryAddress?<div style={{display:'flex',gap:10,alignItems:'center',flexWrap:'wrap'}}><code style={{fontSize:15,color:'#c8d6cc',overflowWrap:'anywhere',flex:'1 1 420px'}}>{data.rpc.treasuryAddress}</code><button type="button" className={styles.secondaryLink} style={{cursor:'pointer'}} onClick={()=>copyAddress(data.rpc.treasuryAddress)}>{copied===data.rpc.treasuryAddress?'Copied':'Copy address'}</button></div>:<p style={{color:'#f5c451'}}>Treasury address unavailable. Connect Yerbas Core or set YERB_TREASURY_ADDRESS.</p>}<p style={{margin:'10px 0 0',fontSize:12,color:'#9aa69d'}}>This is the server treasury/reward wallet address, separate from player deposit addresses.</p></article><div className={styles.grid}><article className={styles.card} style={{minHeight:150,borderColor:data.rpc?.connected?'rgba(103,214,110,.45)':'rgba(245,196,81,.45)'}}><span className={styles.eyebrow}>Yerbas Core RPC</span><h3 style={{fontSize:28}}>{data.rpc?.connected?'Connected':data.rpc?.configured?'RPC error':'Not configured'}</h3>{data.rpc?.connected?<><p>Block {Number(data.rpc.blocks||0).toLocaleString()} · {data.rpc.chain||'main'}</p><p style={{color:'#9aa69d'}}>{Number(data.rpc.walletBalanceYerb||0).toLocaleString()} YERB wallet balance · {Number(data.rpc.txCount||0).toLocaleString()} transactions</p></>:<p style={{color:'#9aa69d'}}>{data.rpc?.error||'RPC status unavailable.'}</p>}</article>{[['Ledger balance',`${data.summary.ledgerBalanceYerb.toLocaleString()} YERB`],['Confirmed deposits',`${data.summary.confirmedDepositsYerb.toLocaleString()} YERB`],['Sent withdrawals',`${data.summary.sentWithdrawalsYerb.toLocaleString()} YERB`],['Posted rewards',`${data.summary.postedRewardsYerb.toLocaleString()} YERB`],['Wallets',data.summary.wallets],['Active deposit addresses',data.summary.activeAddresses],['Pending deposits',data.summary.pendingDeposits],['Pending withdrawals',data.summary.pendingWithdrawals],['Pending rewards',data.summary.pendingRewards]].map(([label,value])=><article className={styles.card} key={String(label)} style={{minHeight:150}}><span className={styles.eyebrow}>{label}</span><h3 style={{fontSize:28}}>{value}</h3></article>)}</div></section><section className={styles.section}><div className={styles.sectionHead}><div><span className={styles.eyebrow}>DEPOSIT ADDRESSES</span><h2>Player deposit addresses</h2></div><span className={styles.count}>{(data.depositAddresses||[]).filter((a:any)=>a.active).length} active</span></div><article className={styles.card} style={{minHeight:0,marginBottom:14}}><span className={styles.eyebrow}>ASSIGN / CREATE</span><h3>Assign/Create Deposit Address</h3><div style={{display:'flex',gap:10,flexWrap:'wrap'}}><select value={selectedWallet} onChange={e=>setSelectedWallet(e.target.value)} style={{minWidth:320,padding:11,background:'#0f1411',color:'#fff'}}>{(data.walletUsers||[]).map((u:any)=><option key={u.walletId} value={u.walletId}>{u.displayName||u.username||u.yerbasAddress||u.userId}{u.activeDepositAddress?' · address assigned':''}</option>)}</select><button type="button" className={styles.primaryLink} disabled={!selectedWallet||busy||!data.rpc?.connected} onClick={assignAddress}>{busy?'Creating…':'Assign/Create Deposit Address'}</button></div>{notice&&<div style={{marginTop:12}}>{notice}</div>}</article><article className={styles.card} style={{minHeight:0}}><div style={{display:'grid',gap:8,maxHeight:460,overflow:'auto'}}>{data.depositAddresses?.length?data.depositAddresses.map((a:any)=><div key={a.id} style={{padding:'12px 0',borderTop:'1px solid rgba(255,255,255,.06)',display:'grid',gridTemplateColumns:'minmax(150px,1fr) minmax(300px,2fr) auto',gap:12}}><div><strong>{a.display_name||a.username||a.user_id}</strong><div>{a.address_type||'deposit'} · {a.active?'active':'inactive'}</div></div><code>{a.address}</code><button type="button" className={styles.secondaryLink} onClick={()=>copyAddress(a.address)}>{copied===a.address?'Copied':'Copy'}</button></div>):<span>No deposit addresses have been assigned yet.</span>}</div></article></section><section className={styles.section}><div className={styles.grid}>{[['Recent deposits',data.recentDeposits,'detected_at'],['Recent withdrawals',data.recentWithdrawals,'requested_at'],['Recent ledger activity',data.recentLedger,'created_at']].map(([title,rows,dateKey]:any)=><article className={styles.card} key={title}><h3>{title}</h3><div style={{display:'grid',gap:8,maxHeight:420,overflow:'auto'}}>{rows.length?rows.map((r:any)=><div key={r.id} style={{padding:'10px 0',borderTop:'1px solid rgba(255,255,255,.06)',fontSize:12}}><strong>{r.status||r.entry_type}</strong><div>{r.amountYerb!==undefined?`${r.amountYerb} YERB`:''}{r.feeYerb?` + ${r.feeYerb} fee`:''}</div><div>{r.user_id||''} · {fmt(r[dateKey])}</div></div>):<span>No records</span>}</div></article>)}</div></section></>}</main>}
