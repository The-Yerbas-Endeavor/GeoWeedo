'use client';

import {useEffect,useMemo,useState} from 'react';
import SiteHeader from '@/components/SiteHeader';

type GameKey='classic'|'hunt'|'daily';
type Leader={rank:number;gameId:string;player:string;score:number;earnedYerb:number;rewardStatus:string;completedAt:string};
type Payload={date:string;boards:Record<GameKey,Leader[]>;limits:Record<GameKey,number>;periods:Record<GameKey,string>};

const GAME_META:Record<GameKey,{icon:string;name:string;description:string;href:string}>={
 classic:{icon:'🎮',name:'Classic GeoWeedo',description:'Five Street View rounds. Scores are ranked out of 25,000.',href:'/'},
 hunt:{icon:'🌿',name:'Weedo Hunt',description:'Track down the hidden dispensary. Scores are ranked out of 5,000.',href:'/hunt'},
 daily:{icon:'🌎',name:'Daily Weedo',description:'One shared challenge every day. Today’s scores are ranked out of 5,000.',href:'/daily'},
};

function initialGame():GameKey{if(typeof window==='undefined')return'classic';const value=new URLSearchParams(window.location.search).get('game');return value==='hunt'||value==='daily'||value==='classic'?value:'classic';}
function rewardStatus(value:string){return value==='held'||value==='pending'?'Pending review':value==='posted'?'Earned':value==='daily_cap_reached'?'Daily cap reached':value.replaceAll('_',' ');}

export default function LeaderboardPage(){
 const[active,setActive]=useState<GameKey>('classic'),[data,setData]=useState<Payload|null>(null),[error,setError]=useState('');
 useEffect(()=>{setActive(initialGame());fetch('/api/leaderboard?limit=50',{cache:'no-store'}).then(async response=>{const body=await response.json().catch(()=>({}));if(!response.ok)throw new Error(body.error||'Could not load the leaderboard.');setData(body);}).catch(err=>setError(err instanceof Error?err.message:'Could not load the leaderboard.'));},[]);
 const select=(game:GameKey)=>{setActive(game);const url=new URL(window.location.href);url.searchParams.set('game',game);window.history.replaceState({},'',url.pathname+url.search);};
 const leaders=useMemo(()=>data?.boards?.[active]||[],[data,active]),meta=GAME_META[active],max=data?.limits?.[active]||0,period=data?.periods?.[active]||'';
 return <main className="landing-shell"><SiteHeader/><section className="leaderboard-page">
  <header className="leaderboard-hero"><div><div className="eyebrow">GEOWEEDO COMPETITION</div><h1>🏆 Weedo Leaderboard</h1><p>Top GeoWeedo scores and the YERB actually earned from completed games. Classic, Weedo Hunt and Daily Weedo all report from the same authoritative game records.</p></div><a className="secondary" href="/">Back to GeoWeedo</a></header>
  <div className="leaderboard-games" role="tablist" aria-label="Choose leaderboard game">{(Object.keys(GAME_META) as GameKey[]).map(game=>{const item=GAME_META[game],count=data?.boards?.[game]?.length||0;return <button key={game} type="button" role="tab" aria-selected={active===game} className={`leaderboard-game-card ${active===game?'active':''}`} onClick={()=>select(game)}><span className="leaderboard-game-icon">{item.icon}</span><span><strong>{item.name}</strong><small>{item.description}</small></span><b>{count} listed</b></button>;})}</div>
  <section className="leaderboard-board result-card"><div className="leaderboard-board-head"><div><div className="eyebrow">{period||'LEADERBOARD'}</div><h2>{meta.icon} {meta.name}</h2><p>{max.toLocaleString()} maximum points</p></div><a className="secondary" href={meta.href}>Play {meta.name}</a></div>
   {error?<p>{error}</p>:!data?<p>Loading leaderboard…</p>:leaders.length===0?<div className="leaderboard-empty"><strong>No scores yet.</strong><span>Complete a {meta.name} game while signed in to claim the first spot.</span></div>:<div className="leaderboard-table"><div className="leaderboard-row header"><span>Rank</span><span>Player</span><span>Score</span><span>Earned YERB</span><span>Status</span></div>{leaders.map(item=><div className="leaderboard-row" key={item.gameId}><strong className={item.rank<=3?'podium':''}>#{item.rank}</strong><span>{item.player}</span><strong>{item.score.toLocaleString()} <small>/ {max.toLocaleString()}</small></strong><strong className="yerb">{item.earnedYerb.toFixed(4)} YERB</strong><span className="leaderboard-status">{rewardStatus(item.rewardStatus)}</span></div>)}</div>}
  </section>
 </section><style jsx global>{`
.leaderboard-page{max-width:1180px;margin:0 auto;padding:30px 22px 52px}.leaderboard-hero{display:flex;align-items:flex-end;justify-content:space-between;gap:24px;margin-bottom:20px}.leaderboard-hero h1{font-size:clamp(34px,5vw,54px);margin:5px 0 8px;line-height:1}.leaderboard-hero p{max-width:760px;margin:0;color:#cbd5cd;line-height:1.55}.leaderboard-games{display:grid;grid-template-columns:repeat(3,1fr);gap:12px;margin-bottom:16px}.leaderboard-game-card{display:grid;grid-template-columns:auto 1fr;grid-template-rows:auto auto;gap:4px 12px;text-align:left;min-height:128px;padding:18px;border:1px solid rgba(255,255,255,.12);border-radius:16px;background:#101612;color:var(--text);cursor:pointer}.leaderboard-game-card:hover,.leaderboard-game-card.active{border-color:#67d66e;background:rgba(103,214,110,.08)}.leaderboard-game-card.active{box-shadow:inset 0 0 0 1px rgba(103,214,110,.28)}.leaderboard-game-icon{grid-row:1/3;font-size:27px}.leaderboard-game-card strong{display:block;font-size:17px}.leaderboard-game-card small{display:block;color:#aab5ac;margin-top:5px;line-height:1.35}.leaderboard-game-card b{grid-column:2;color:#67d66e;font-size:11px;text-transform:uppercase;letter-spacing:.08em;margin-top:6px}.leaderboard-board{width:100%!important;max-width:none!important;margin:0!important;padding:24px!important;text-align:left!important}.leaderboard-board-head{display:flex;justify-content:space-between;gap:18px;align-items:flex-end;margin-bottom:16px}.leaderboard-board-head h2{margin:4px 0;font-size:28px}.leaderboard-board-head p{margin:0;color:#9aa69d}.leaderboard-board-head a{text-decoration:none}.leaderboard-table{width:100%;border:1px solid rgba(255,255,255,.1);border-radius:13px;overflow-x:auto;overflow-y:hidden}.leaderboard-row{display:grid;grid-template-columns:70px minmax(140px,1fr) 190px 170px 130px;gap:12px;align-items:center;min-width:760px;padding:13px 14px;border-top:1px solid rgba(255,255,255,.07);background:rgba(255,255,255,.02)}.leaderboard-row:first-child{border-top:0}.leaderboard-row.header{font-size:10px;text-transform:uppercase;letter-spacing:.09em;color:#89958c;background:rgba(255,255,255,.05)}.leaderboard-row strong small{font-weight:500;color:#7f8a82}.leaderboard-row .yerb{color:#67d66e}.leaderboard-row .podium{color:#f5c451}.leaderboard-status{text-transform:capitalize;color:#aeb8b0;font-size:12px}.leaderboard-empty{display:grid;gap:6px;padding:30px;border:1px dashed rgba(255,255,255,.14);border-radius:12px;color:#cbd5cd}.leaderboard-empty strong{color:#fff;font-size:18px}@media(max-width:850px){.leaderboard-games{grid-template-columns:1fr}.leaderboard-game-card{min-height:0}.leaderboard-row{grid-template-columns:55px minmax(110px,1fr) 120px 135px;min-width:560px}.leaderboard-row>:last-child{display:none}.leaderboard-row.header>:last-child{display:none}}@media(max-width:560px){.leaderboard-page{padding:20px 10px 36px}.leaderboard-hero,.leaderboard-board-head{align-items:flex-start;flex-direction:column}.leaderboard-row{grid-template-columns:42px minmax(80px,1fr) 90px 105px;min-width:440px;font-size:12px;gap:6px;padding:11px 8px}.leaderboard-row.header{font-size:8px}.leaderboard-row strong small{display:none}.leaderboard-board{padding:14px!important}}
`}</style></main>;
}
