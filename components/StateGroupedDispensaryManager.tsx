'use client';

import { useEffect, useMemo, useState } from 'react';
import AdminCoordinateMap from '@/components/AdminCoordinateMap';

type SavedDispensary={id:string;name:string;streetAddress?:string;city:string;region:string;country:string;latitude:number;longitude:number;website?:string;imageryProvider:string;dataSource?:string;sourceUrl?:string;sourceLicense?:string;recreational:boolean;medical:boolean;active:boolean};
type Candidate={id:string;name:string;streetAddress?:string;city?:string;region?:string;country?:string;latitude?:number;longitude?:number;website?:string;dataSource?:string;sourceUrl?:string;sourceLicense?:string;status?:string;imageryStatus?:string;imageryCount?:number;automatedEnrichmentApproved?:boolean};
type EditForm={name:string;streetAddress:string;city:string;region:string;country:string;website:string;dataSource:string;sourceUrl:string;sourceLicense:string;latitude:string;longitude:string;recreational:boolean;medical:boolean};
type StateItem={kind:'dispensary'|'candidate';id:string;name:string;streetAddress?:string;city:string;region:string;country:string;latitude?:number;longitude?:number;website?:string;dataSource?:string;active?:boolean;imageryProvider?:string;status?:string;imageryStatus?:string;automatedEnrichmentApproved?:boolean;source?:SavedDispensary|Candidate};
type ViewFilter='all'|'enabled'|'disabled'|'candidates'|'enriched'|'rejected';
type SortMode='name'|'city'|'enabled-first'|'candidates-first';
const STATE_ALIASES:Record<string,string>={CA:'California',CALIFORNIA:'California',CO:'Colorado',COLORADO:'Colorado',CT:'Connecticut',CONNECTICUT:'Connecticut',IL:'Illinois',ILLINOIS:'Illinois',MA:'Massachusetts',MASSACHUSETTS:'Massachusetts',MT:'Montana',MONTANA:'Montana',NV:'Nevada',NEVADA:'Nevada',NY:'New York','NEW YORK':'New York','NEW YORK STATE':'New York',OR:'Oregon',OREGON:'Oregon',WA:'Washington',WASHINGTON:'Washington','WASHINGTON STATE':'Washington'};
function normalizeState(value?:string){const raw=(value||'').trim();if(!raw)return'Unknown state';return STATE_ALIASES[raw.toUpperCase()]||raw;}
function editFrom(item:SavedDispensary):EditForm{return{name:item.name,streetAddress:item.streetAddress||'',city:item.city||'',region:item.region||'',country:item.country||'USA',website:item.website||'',dataSource:item.dataSource||'manual',sourceUrl:item.sourceUrl||'',sourceLicense:item.sourceLicense||'',latitude:String(item.latitude),longitude:String(item.longitude),recreational:Boolean(item.recreational),medical:Boolean(item.medical)};}
function itemRank(item:StateItem,mode:SortMode){if(mode==='enabled-first')return item.kind==='dispensary'&&item.active?0:item.kind==='dispensary'?1:2;if(mode==='candidates-first')return item.kind==='candidate'&&item.status!=='rejected'?0:item.kind==='candidate'?1:2;return 0;}

export default function StateGroupedDispensaryManager(){
 const[dispensaries,setDispensaries]=useState<SavedDispensary[]>([]),[candidates,setCandidates]=useState<Candidate[]>([]),[search,setSearch]=useState(''),[view,setView]=useState<ViewFilter>('all'),[sort,setSort]=useState<SortMode>('name'),[expanded,setExpanded]=useState<Record<string,boolean>>({}),[status,setStatus]=useState('Loading dispensaries and candidates…'),[busy,setBusy]=useState(false),[editing,setEditing]=useState<SavedDispensary|null>(null),[form,setForm]=useState<EditForm|null>(null);
 async function load(preserveStatus=false){const [dispensaryResponse,candidateResponse]=await Promise.all([fetch('/api/admin/dispensaries',{cache:'no-store'}),fetch('/api/admin/candidates',{cache:'no-store'})]);if(dispensaryResponse.status===401||candidateResponse.status===401){window.location.href='/admin/login';return;}const [dispensaryData,candidateData]=await Promise.all([dispensaryResponse.json(),candidateResponse.json()]);if(!dispensaryResponse.ok)throw new Error(dispensaryData.error||'Could not load dispensaries.');if(!candidateResponse.ok)throw new Error(candidateData.error||'Could not load imported candidates.');const nextDispensaries=dispensaryData.dispensaries||[],nextCandidates=candidateData.candidates||[];setDispensaries(nextDispensaries);setCandidates(nextCandidates);if(!preserveStatus)setStatus(`${nextDispensaries.length} game-pool dispensaries · ${nextCandidates.length} imported candidates.`);return nextDispensaries as SavedDispensary[];}
 useEffect(()=>{load().catch(e=>setStatus(e.message));const refresh=()=>load().catch(e=>setStatus(e.message));window.addEventListener('focus',refresh);return()=>window.removeEventListener('focus',refresh);},[]);
 const allItems=useMemo<StateItem[]>(()=>[
  ...dispensaries.map(item=>({kind:'dispensary' as const,id:item.id,name:item.name,streetAddress:item.streetAddress,city:item.city||'',region:normalizeState(item.region),country:item.country||'USA',latitude:item.latitude,longitude:item.longitude,website:item.website,dataSource:item.dataSource,active:item.active,imageryProvider:item.imageryProvider,source:item})),
  ...candidates.map(item=>({kind:'candidate' as const,id:item.id,name:item.name,streetAddress:item.streetAddress,city:item.city||'',region:normalizeState(item.region),country:item.country||'USA',latitude:item.latitude,longitude:item.longitude,website:item.website,dataSource:item.dataSource,status:item.status||'candidate',imageryStatus:item.imageryStatus,automatedEnrichmentApproved:Boolean(item.automatedEnrichmentApproved),source:item}))
 ],[dispensaries,candidates]);
 const filtered=useMemo(()=>{const q=search.trim().toLowerCase();return allItems.filter(item=>{const matchesView=view==='all'||(view==='enabled'&&item.kind==='dispensary'&&item.active)||(view==='disabled'&&item.kind==='dispensary'&&!item.active)||(view==='candidates'&&item.kind==='candidate'&&item.status!=='rejected')||(view==='enriched'&&item.kind==='candidate'&&item.status!=='rejected'&&item.automatedEnrichmentApproved)||(view==='rejected'&&item.kind==='candidate'&&item.status==='rejected');if(!matchesView)return false;return !q||`${item.name} ${item.city} ${item.region} ${item.streetAddress||''} ${item.dataSource||''} ${item.status||''}`.toLowerCase().includes(q);});},[allItems,search,view]);
 const groups=useMemo(()=>{const map=new Map<string,StateItem[]>();for(const item of filtered){const state=normalizeState(item.region);const arr=map.get(state);if(arr)arr.push(item);else map.set(state,[item]);}return Array.from(map.entries()).sort(([a],[b])=>a.localeCompare(b)).map(([state,list])=>{const sorted=list.slice().sort((a,b)=>{const rank=itemRank(a,sort)-itemRank(b,sort);if(rank)return rank;if(sort==='city')return a.city.localeCompare(b.city)||a.name.localeCompare(b.name);return a.name.localeCompare(b.name)||a.city.localeCompare(b.city);});return{state,list:sorted,enabled:list.filter(i=>i.kind==='dispensary'&&i.active).length,disabled:list.filter(i=>i.kind==='dispensary'&&!i.active).length,candidateCount:list.filter(i=>i.kind==='candidate'&&i.status!=='rejected').length,enrichedCount:list.filter(i=>i.kind==='candidate'&&i.status!=='rejected'&&i.automatedEnrichmentApproved).length,rejectedCount:list.filter(i=>i.kind==='candidate'&&i.status==='rejected').length};});},[filtered,sort]);
 function toggleState(state:string){setExpanded(current=>({...current,[state]:!current[state]}));}
 function toggleView(next:ViewFilter){setView(current=>current===next?'all':next);}
 function beginEdit(item:StateItem){if(item.kind!=='dispensary')return;const source=item.source as SavedDispensary;setEditing(source);setForm(editFrom(source));window.setTimeout(()=>document.getElementById('state-dispensary-edit')?.scrollIntoView({behavior:'smooth',block:'start'}),0);}
 function reviewCandidate(item:StateItem){if(item.kind!=='candidate')return;sessionStorage.setItem('geoweedo-candidate-draft',JSON.stringify(item.source));window.location.href='/admin/dispensaries';}
 async function enrichCandidate(item:StateItem){if(item.kind!=='candidate'||item.status==='rejected')return;setBusy(true);setStatus(`${item.name}: running Google Places Automated Enrichment…`);try{const r=await fetch('/api/admin/dispensary-batch-enrichment',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'singleCandidate',locationId:item.id,autoApply:true})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Automated Enrichment failed.');const counts=d.job?.counts||{};let nextMessage=`${item.name}: Automated Enrichment finished.`;if(Number(counts.applied||0)>0)nextMessage=`${item.name}: Google Places enrichment applied.`;else if(Number(counts.review||0)>0)nextMessage=`${item.name}: Google Places match needs Admin Review.`;else if(Number(counts.failed||0)>0)nextMessage=`${item.name}: Automated Enrichment failed. Check the batch result for details.`;await load(true);setStatus(nextMessage);}catch(e){setStatus(e instanceof Error?e.message:'Automated Enrichment failed.');}finally{setBusy(false);}}
 async function enrichEditingDispensary(){if(!editing)return;setBusy(true);setStatus(`${editing.name}: running Google Places Automated Enrichment…`);try{const r=await fetch('/api/admin/dispensary-batch-enrichment',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'singleDispensary',locationId:editing.id,autoApply:true})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Automated Enrichment failed.');const counts=d.job?.counts||{};const refreshed=await load(true);const next=refreshed?.find(item=>item.id===editing.id);if(next){setEditing(next);setForm(editFrom(next));}if(Number(counts.applied||0)>0)setStatus(`${editing.name}: Google Places enrichment applied and editor refreshed.`);else if(Number(counts.review||0)>0)setStatus(`${editing.name}: Google Places match needs Admin Review before changes are applied.`);else if(Number(counts.failed||0)>0)setStatus(`${editing.name}: Automated Enrichment failed. Check the batch result for details.`);else setStatus(`${editing.name}: Automated Enrichment finished.`);}catch(e){setStatus(e instanceof Error?e.message:'Automated Enrichment failed.');}finally{setBusy(false);}}
 async function approveCandidateIds(ids:string[]){if(!ids.length)return{promoted:0,skipped:0};const r=await fetch('/api/admin/candidates',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({ids,action:'approve'})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Candidate promotion failed.');return{promoted:Number(d.promoted||0),skipped:Number(d.skipped||0)};}
 async function enableEnriched(){if(!enrichedTotal)return;setBusy(true);setStatus('Enabling Automated Enrichment approved candidates…');let checked=0,passed=0,promoted=0,skipped=0;try{const alreadyReady=candidates.filter(i=>i.status!=='rejected'&&i.automatedEnrichmentApproved&&i.imageryStatus==='coverage').map(i=>i.id);if(alreadyReady.length){const result=await approveCandidateIds(alreadyReady);promoted+=result.promoted;skipped+=result.skipped;}for(let batch=0;batch<5;batch++){const r=await fetch('/api/admin/candidates/check-imagery',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({limit:50,source:'enrichment_approved'})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Street View validation failed.');const results=(Array.isArray(d.results)?d.results:[]).filter(Boolean) as Candidate[];const count=Number(d.checked||results.length||0);checked+=count;if(!count)break;const ids=results.filter(i=>i.imageryStatus==='coverage').map(i=>i.id);passed+=ids.length;if(ids.length){const result=await approveCandidateIds(ids);promoted+=result.promoted;skipped+=result.skipped;}}await load(true);setStatus(`Enriched enable pass complete: ${checked} Street View checks · ${passed} passed · ${promoted} enabled${skipped?` · ${skipped} skipped during final validation`:''}.`);window.dispatchEvent(new Event('geoweedo-pipeline-updated'));}catch(e){setStatus(e instanceof Error?e.message:'Could not enable enriched candidates.');}finally{setBusy(false);}}
 async function toggle(item:StateItem){if(item.kind!=='dispensary')return;const source=item.source as SavedDispensary;setBusy(true);try{const r=await fetch('/api/admin/dispensaries',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({id:source.id,active:!source.active})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||'Status update failed.');await load(true);setStatus(`${source.name} ${source.active?'deactivated':'activated'}.`);}catch(e){setStatus(e instanceof Error?e.message:'Status update failed.');}finally{setBusy(false);}}
 async function saveEdit(){if(!editing||!form)return;setBusy(true);try{const latitude=Number(form.latitude),longitude=Number(form.longitude);if(!form.name.trim()||!form.city.trim()||!form.region.trim()||!form.country.trim())throw new Error('Name, city, state, and country are required.');if(!Number.isFinite(latitude)||!Number.isFinite(longitude)||latitude<-90||latitude>90||longitude<-180||longitude>180)throw new Error('Enter valid latitude and longitude.');const r=await fetch('/api/admin/dispensaries',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({id:editing.id,details:{...form,region:normalizeState(form.region),latitude,longitude}})});const d=await r.json();if(!r.ok)throw new Error(d.error||'Dispensary update failed.');setEditing(null);setForm(null);await load(true);setStatus(`${d.dispensary.name} details updated.`);}catch(e){setStatus(e instanceof Error?e.message:'Dispensary update failed.');}finally{setBusy(false);}}
 const enabledTotal=dispensaries.filter(i=>i.active).length,disabledTotal=dispensaries.filter(i=>!i.active).length,candidateTotal=candidates.filter(i=>i.status!=='rejected').length,enrichedTotal=candidates.filter(i=>i.status!=='rejected'&&i.automatedEnrichmentApproved).length,rejectedTotal=candidates.filter(i=>i.status==='rejected').length;
 const editLatitude=form?Number(form.latitude):NaN,editLongitude=form?Number(form.longitude):NaN;
 return <section className="admin-panel state-grouped-dispensaries">
  <style>{`
   .state-grouped-dispensaries{margin-bottom:24px!important;padding:22px!important;background:#111714!important;border:1px solid #2b3830!important;color:#f2f7f3!important;scroll-margin-top:18px}
   .state-grouped-head{display:flex;justify-content:space-between;gap:18px;align-items:flex-end;flex-wrap:wrap;margin-bottom:14px}.state-grouped-head h2{font-size:26px;line-height:1.1;margin:4px 0 8px;color:#fff}.state-grouped-head .eyebrow{font-size:12px;letter-spacing:.12em;color:#76e27f;font-weight:800}.state-grouped-summary{display:flex;gap:8px;flex-wrap:wrap;margin-top:10px}.state-stat{display:inline-flex;align-items:center;min-height:30px;padding:5px 10px;border-radius:999px;background:#18221c;border:1px solid #314137;font-size:12px;color:#d9e7dd;font-weight:700}.state-stat-button{cursor:pointer;appearance:none;font:inherit;transition:.15s ease}.state-stat-button:hover{border-color:#76e27f;background:#1d2b22;color:#fff}.state-stat-button.active{background:#24472b;border-color:#76e27f;color:#8df497;box-shadow:0 0 0 1px rgba(118,226,127,.16) inset}
   .state-manager-tools{display:grid;grid-template-columns:minmax(280px,1fr) 180px 190px;gap:8px;width:min(850px,100%);margin-left:auto}.state-manager-tools input,.state-manager-tools select{min-width:0;background:#0b100d!important;color:#fff!important;border:1px solid #3c5142!important;border-radius:9px;padding:11px 13px!important}.state-manager-tools option{background:#0b100d;color:#fff}.state-grouped-status{margin:0 0 16px;padding:10px 12px;border-radius:8px;background:#0b100d;color:#aec2b4;border:1px solid #253229;font-size:12px}
   .state-edit-card{scroll-margin-top:18px;margin-bottom:18px;padding:16px;border:1px solid #52745d;border-radius:12px;background:#0b100d}.state-edit-layout{display:grid;grid-template-columns:minmax(0,1.2fr) minmax(360px,.8fr);gap:18px;align-items:start}.state-edit-map-panel{border:1px solid #314137;border-radius:12px;overflow:hidden;background:#0f1512}.state-edit-map-head{padding:11px 13px;border-bottom:1px solid #314137}.state-edit-map-head strong{display:block;color:#fff;font-size:13px}.state-edit-map-head span{display:block;color:#8fa297;font-size:11px;margin-top:3px}.admin-coordinate-map{height:390px;width:100%;background:#18211b}.state-edit-coordinates{padding:9px 12px;border-top:1px solid #314137;color:#a9bbb0;font-size:11px}.state-edit-actions{display:grid;grid-template-columns:minmax(0,1fr) minmax(220px,.45fr);gap:10px;align-items:stretch}.state-edit-actions button{width:100%}
   .state-groups{display:grid;grid-template-columns:repeat(auto-fit,minmax(360px,1fr));gap:12px;align-items:start}.state-card{border:1px solid #314137;border-radius:12px;background:#0c120f;overflow:hidden;min-width:0}.state-card-head{width:100%;display:flex;justify-content:space-between;align-items:center;gap:16px;padding:16px 18px!important;text-align:left!important;background:#151e18!important;color:#fff!important;border:0!important;border-radius:0!important}.state-card-head:hover{background:#1b2820!important}.state-name{font-size:18px;font-weight:800;line-height:1.2}.state-counts{display:flex;gap:6px;flex-wrap:wrap;margin-top:7px}.state-counts span{font-size:11px;padding:3px 7px;border-radius:999px;background:#0d140f;border:1px solid #304038;color:#bdcec2}.state-counts .enabled{color:#7cf088;border-color:#315e39}.state-counts .candidate{color:#f2d67a;border-color:#66582c}.state-counts .enriched{color:#8ecbff;border-color:#355b78}.state-counts .rejected{color:#e5aaa5;border-color:#6b3f3a}.state-chevron{font-size:22px;color:#76e27f;line-height:1}
   .state-list{border-top:1px solid #28352d;max-height:520px;overflow-y:auto;overscroll-behavior:contain;scrollbar-width:thin;scrollbar-color:#435b4a #0c120f}.state-list::-webkit-scrollbar{width:9px}.state-list::-webkit-scrollbar-track{background:#0c120f}.state-list::-webkit-scrollbar-thumb{background:#435b4a;border-radius:8px}.state-list-summary{position:sticky;top:0;z-index:2;padding:8px 14px;background:#0b100dec;border-bottom:1px solid #28352d;color:#8fa297;font-size:11px;backdrop-filter:blur(8px)}.state-item{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:16px;align-items:center;padding:13px 16px;border-bottom:1px solid #202b25;background:#0f1512}.state-item:last-child{border-bottom:0}.state-item strong{display:block;color:#fff;font-size:14px;margin-bottom:4px}.state-item-meta{display:block;color:#b3c3b8;font-size:12px;line-height:1.45}.state-item-detail{display:block;color:#829289;font-size:11px;margin-top:3px;line-height:1.4;word-break:break-word}.state-item-actions{display:flex;gap:7px;flex-wrap:wrap;justify-content:flex-end}.state-item-actions button{white-space:nowrap}.state-badge{display:inline-flex;margin-left:7px;padding:2px 6px;border-radius:999px;font-size:9px;vertical-align:1px;border:1px solid #3b4c41;color:#b9c8bd}.state-badge.live{color:#7cf088;border-color:#315e39}.state-badge.candidate{color:#f2d67a;border-color:#66582c}.state-badge.enriched{color:#8ecbff;border-color:#355b78}.state-badge.disabled{color:#c7aaa7;border-color:#60413e}.state-badge.rejected{color:#e5aaa5;border-color:#6b3f3a}
   @media(max-width:1100px){.state-edit-layout{grid-template-columns:1fr}.admin-coordinate-map{height:340px}}@media(max-width:900px){.state-manager-tools{grid-template-columns:1fr 1fr}.state-manager-tools input{grid-column:1/-1}}@media(max-width:760px){.state-groups{grid-template-columns:1fr}.state-manager-tools{grid-template-columns:1fr}.state-manager-tools input{grid-column:auto}.state-item{grid-template-columns:1fr}.state-item-actions{justify-content:flex-start}.state-list{max-height:440px}.admin-coordinate-map{height:300px}.state-edit-actions{grid-template-columns:1fr}}
  `}</style>
  <div className="state-grouped-head"><div><span className="eyebrow">DISPENSARIES BY STATE</span><h2>State location manager</h2><div className="state-grouped-summary"><span className="state-stat">{groups.length} states</span><button type="button" className={`state-stat state-stat-button ${view==='enabled'?'active':''}`} onClick={()=>toggleView('enabled')} aria-pressed={view==='enabled'} title="Show enabled dispensaries">{enabledTotal} enabled</button><span className="state-stat">{disabledTotal} disabled</span><button type="button" className={`state-stat state-stat-button ${view==='candidates'?'active':''}`} onClick={()=>toggleView('candidates')} aria-pressed={view==='candidates'} title="Show imported candidates">{candidateTotal} candidates</button><button type="button" className={`state-stat state-stat-button ${view==='enriched'?'active':''}`} onClick={()=>toggleView('enriched')} aria-pressed={view==='enriched'} title="Show candidates that passed Automated Enrichment">{enrichedTotal} enrichment approved</button><button type="button" className="state-stat state-stat-button" disabled={busy||enrichedTotal===0} onClick={()=>void enableEnriched()} title="Validate Street View and enable enrichment-approved candidates">Enable enriched</button>{rejectedTotal>0&&<span className="state-stat">{rejectedTotal} rejected</span>}<span className="state-stat">{filtered.length} shown</span></div></div><div className="state-manager-tools"><input placeholder="Search name, city, state, address, source, or status" value={search} onChange={e=>setSearch(e.target.value)}/><select value={view} onChange={e=>setView(e.target.value as ViewFilter)} aria-label="Filter locations"><option value="all">View: All</option><option value="enabled">View: Enabled</option><option value="candidates">View: Candidates</option><option value="enriched">View: Enrichment approved</option><option value="disabled">View: Disabled</option><option value="rejected">View: Rejected</option></select><select value={sort} onChange={e=>setSort(e.target.value as SortMode)} aria-label="Sort locations"><option value="name">Sort: Name A–Z</option><option value="city">Sort: City A–Z</option><option value="enabled-first">Sort: Enabled first</option><option value="candidates-first">Sort: Candidates first</option></select></div></div>
  <div className="state-grouped-status">{status}</div>
  {editing&&form&&<div id="state-dispensary-edit" className="state-edit-card"><div style={{display:'flex',justifyContent:'space-between',alignItems:'center',gap:12,marginBottom:14}}><div><span className="eyebrow">EDIT DISPENSARY</span><strong style={{display:'block',marginTop:4,fontSize:17}}>{editing.name}</strong></div><button type="button" onClick={()=>{setEditing(null);setForm(null);}}>Cancel</button></div><div className="state-edit-layout"><div className="admin-form"><input placeholder="Dispensary name" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/><input placeholder="Street address" value={form.streetAddress} onChange={e=>setForm({...form,streetAddress:e.target.value})}/><div className="field-row"><input placeholder="City" value={form.city} onChange={e=>setForm({...form,city:e.target.value})}/><input placeholder="State / region" value={form.region} onChange={e=>setForm({...form,region:e.target.value})}/></div><div className="field-row"><input placeholder="Country" value={form.country} onChange={e=>setForm({...form,country:e.target.value})}/><input placeholder="Website" value={form.website} onChange={e=>setForm({...form,website:e.target.value})}/></div><div className="field-row"><input placeholder="Latitude" value={form.latitude} onChange={e=>setForm({...form,latitude:e.target.value})}/><input placeholder="Longitude" value={form.longitude} onChange={e=>setForm({...form,longitude:e.target.value})}/></div><div className="field-row"><input placeholder="Data source" value={form.dataSource} onChange={e=>setForm({...form,dataSource:e.target.value})}/><input placeholder="Source URL" value={form.sourceUrl} onChange={e=>setForm({...form,sourceUrl:e.target.value})}/></div><input placeholder="Source license / permission" value={form.sourceLicense} onChange={e=>setForm({...form,sourceLicense:e.target.value})}/><div className="check-row"><label><input type="checkbox" checked={form.recreational} onChange={e=>setForm({...form,recreational:e.target.checked})}/> Recreational</label><label><input type="checkbox" checked={form.medical} onChange={e=>setForm({...form,medical:e.target.checked})}/> Medical</label></div><div className="state-edit-actions"><button className="primary" type="button" disabled={busy} onClick={saveEdit}>{busy?'Working…':'Save details'}</button><button className="secondary" type="button" disabled={busy} onClick={()=>void enrichEditingDispensary()}>{busy?'Working…':'Run Google Places enrichment'}</button></div><small style={{display:'block',color:'#8fa297'}}>High-confidence Google Places matches apply automatically and refresh this editor. Uncertain matches are sent to Admin Review.</small></div><div className="state-edit-map-panel"><div className="state-edit-map-head"><strong>Verify coordinates</strong><span>Click the map or drag the green pin to correct the saved location.</span></div>{Number.isFinite(editLatitude)&&Number.isFinite(editLongitude)?<><AdminCoordinateMap key={editing.id} latitude={editLatitude} longitude={editLongitude} onChange={(latitude,longitude)=>setForm(current=>current?{...current,latitude:latitude.toFixed(6),longitude:longitude.toFixed(6)}:current)}/><div className="state-edit-coordinates">{editLatitude.toFixed(6)}, {editLongitude.toFixed(6)}</div></>:<div style={{padding:18,color:'#e5aaa5'}}>Enter valid latitude and longitude to load the verification map.</div>}</div></div></div>}
  <div className="state-groups">{groups.length===0?<p>No dispensaries or candidates match the active filters.</p>:groups.map(group=>{const open=Boolean(expanded[group.state]);return <div className="state-card" key={group.state}><button className="state-card-head" type="button" onClick={()=>toggleState(group.state)} aria-expanded={open}><span><span className="state-name">{group.state}</span><span className="state-counts"><span>{group.list.length} shown</span>{group.enabled>0&&<span className="enabled">{group.enabled} enabled</span>}{group.disabled>0&&<span>{group.disabled} disabled</span>}{group.candidateCount>0&&<span className="candidate">{group.candidateCount} candidates</span>}{group.enrichedCount>0&&<span className="enriched">{group.enrichedCount} enrichment approved</span>}{group.rejectedCount>0&&<span className="rejected">{group.rejectedCount} rejected</span>}</span></span><b className="state-chevron">{open?'−':'+'}</b></button>{open&&<div className="state-list"><div className="state-list-summary">{group.list.length.toLocaleString()} locations · scroll this state independently</div>{group.list.map(item=><div className="state-item" key={`${item.kind}-${item.id}`}><div><strong>{item.name}{item.kind==='candidate'?(item.status==='rejected'?<span className="state-badge rejected">REJECTED</span>:<><span className="state-badge candidate">{(item.status||'candidate').toUpperCase()}</span>{item.automatedEnrichmentApproved&&<span className="state-badge enriched">ENRICHED</span>}</>):item.active?<span className="state-badge live">ENABLED</span>:<span className="state-badge disabled">DISABLED</span>}</strong><span className="state-item-meta">{item.city||'City pending'}{item.imageryProvider?` · ${item.imageryProvider}`:''}{item.imageryStatus?` · imagery ${item.imageryStatus}`:''}{item.dataSource?` · ${item.dataSource}`:''}</span><span className="state-item-detail">{item.streetAddress||'Address pending'}{Number.isFinite(item.latitude)&&Number.isFinite(item.longitude)?` · ${Number(item.latitude).toFixed(5)}, ${Number(item.longitude).toFixed(5)}`:' · coordinates pending'}</span></div><div className="state-item-actions">{item.kind==='candidate'?(item.status==='rejected'?<button type="button" onClick={()=>reviewCandidate(item)}>Review / restore</button>:<button type="button" disabled={busy||item.automatedEnrichmentApproved} onClick={()=>void enrichCandidate(item)}>{item.automatedEnrichmentApproved?'Enrichment approved':'Run enrichment'}</button>):<><button type="button" onClick={()=>beginEdit(item)}>Edit dispensary</button><button type="button" disabled={busy} onClick={()=>toggle(item)}>{item.active?'Deactivate':'Activate'}</button></>}</div></div>)}</div>}</div>;})}</div>
 </section>;
}
