'use client';

export default function MapBrowserPanelDraggable(){
  // The homepage browse panel now has a deliberate fixed desktop position on the
  // left so the selected state can be framed in the unobstructed map area to the
  // right. Keep this component as a no-op for compatibility with the root layout.
  return null;
}
