export type MaineCandidate={name:string;streetAddress?:string;city?:string;region:string;country:string;licenseNumber?:string;dataSource:string;sourceUrl:string;sourceLicense:string;imageryStatus:'missing_coordinates'};
const SOURCE_URL='https://www.maine.gov/dafs/ocp/open-data/adult-use/licensee-search';
function parseCsv(text:string){const rows:string[][]=[];let row:string[]=[],field='',quoted=false;for(let i=0;i<text.length;i++){const ch=text[i];if(ch==='"'){if(quoted&&text[i+1]==='"'){field+='"';i++;}else quoted=!quoted;}else if(ch===','&&!quoted){row.push(field);field='';}else if((ch==='\n'||ch==='\r')&&!quoted){if(ch==='\r'&&text[i+1]==='\n')i++;row.push(field);field='';if(row.some(v=>v.trim()))rows.push(row);row=[];}else field+=ch;}if(field||row.length){row.push(field);if(row.some(v=>v.trim()))rows.push(row);}return rows;}
function key(v:string){return v.toLowerCase().replace(/[^a-z0-9]/g,'');}
function decodeHref(value:string){return value.replace(/&amp;/g,'&').replace(/&#38;/g,'&');}
async function currentCsvUrl(){const response=await fetch(SOURCE_URL,{headers:{Accept:'text/html,application/xhtml+xml','User-Agent':'GeoWeedo/0.8 (https://geoweedo.com)'},cache:'no-store',signal:AbortSignal.timeout(30000)});if(!response.ok)throw new Error(`Maine OCP licensee page returned ${response.status}`);const html=await response.text();const re=/href=["']([^"']+\.csv(?:\?[^"']*)?)["']/gi;let match:RegExpExecArray|null;const urls:string[]=[];while((match=re.exec(html))!==null){const href=decodeHref(match[1]);if(/adult|use|license|applicant|establish/i.test(href))urls.push(new URL(href,SOURCE_URL).toString());}if(!urls.length)throw new Error('Maine OCP page exposed no downloadable adult-use CSV; refusing a stale import.');return urls[0];}
export async function fetchMaineCandidates():Promise<MaineCandidate[]>{const csvUrl=await currentCsvUrl();const response=await fetch(csvUrl,{headers:{Accept:'text/csv,*/*','User-Agent':'GeoWeedo/0.8 (https://geoweedo.com)'},cache:'no-store',signal:AbortSignal.timeout(30000)});if(!response.ok)throw new Error(`Maine OCP adult-use CSV returned ${response.status}`);const parsed=parseCsv((await response.text()).replace(/^\uFEFF/,''));if(parsed.length<2)throw new Error('Maine OCP adult-use CSV contained no records.');let headerIndex=-1;for(let i=0;i<Math.min(parsed.length,25);i++){const ks=parsed[i].map(key);if(ks.includes('licensetype')&&ks.includes('licensestatus')&&(ks.includes('dba')||ks.includes('licensename'))){headerIndex=i;break;}}if(headerIndex<0)throw new Error(`Maine OCP CSV schema was not recognized. First row: ${parsed[0].slice(0,8).join(' | ')}`);const headers=parsed[headerIndex].map(key);const get=(row:string[],names:string[])=>{for(const name of names){const i=headers.indexOf(key(name));if(i>=0&&row[i]?.trim())return row[i].trim();}return '';};const rows:MaineCandidate[]=[];for(const raw of parsed.slice(headerIndex+1)){const category=get(raw,['LICENSE_CATEGORY','License Category']),type=get(raw,['LICENSE_TYPE','License Type']),status=get(raw,['LICENSE_STATUS','License Status']);const retailText=`${category} ${type}`;if(!/retail|store/i.test(retailText)||!/active|issued|current/i.test(status)||/inactive|expired|revoked|surrendered|denied|pending|conditional/i.test(status))continue;const name=get(raw,['DBA','LICENSE_NAME','License Name']);const streetAddress=get(raw,['LICENSE_ADDRESS','License Address']);const city=get(raw,['LICENSE_CITY','License City']);const licenseNumber=get(raw,['LICENSE','License Number']);if(!name||!city)continue;rows.push({name,streetAddress:streetAddress||undefined,city,region:'Maine',country:'USA',licenseNumber:licenseNumber||undefined,dataSource:'Maine OCP Adult Use Applicant and Licensee Data',sourceUrl:SOURCE_URL,sourceLicense:'Official Maine Office of Cannabis Policy downloadable adult-use licensee data; active retail establishments only.',imageryStatus:'missing_coordinates'});}const unique=new Map<string,MaineCandidate>();for(const row of rows){const id=(row.licenseNumber||`${row.name}|${row.streetAddress||''}|${row.city||''}`).toLowerCase();if(!unique.has(id))unique.set(id,row);}if(!unique.size){const sample=parsed.slice(headerIndex+1,headerIndex+8).map(r=>`${get(r,['LICENSE_CATEGORY'])}/${get(r,['LICENSE_TYPE'])}/${get(r,['LICENSE_STATUS'])}`).join(' ; ');throw new Error(`Maine OCP returned zero active retail records. Sample category/type/status: ${sample}`);}return Array.from(unique.values());}
