import 'server-only';
import {getLocationBase,getCommunityProfile,upsertCommunityProfile} from '@/lib/dispensaryCommunity';import {getDatabase} from '@/lib/sqlite';
export type SiteCandidate={url:string;title:string;snippet:string;score:number;confidence:'high'|'medium'|'low';reasons:string[]};
const BLOCKED=['weedmaps.com','leafly.com','yelp.com','facebook.com','instagram.com','x.com','twitter.com','google.com','mapquest.com','yellowpages.com','dutchie.com'];
function norm(v:unknown){return String(v||'').toLowerCase().replace(/[^a-z0-9]+/g,' ').trim();}function tokens(v:unknown){return new Set(norm(v).split(/\s+/).filter(x=>x.length>2));}function overlap(a:Set<string>,b:Set<string>){if(!a.size)return 0;let n=0;a.forEach(x=>{if(b.has(x))n++;});return n/a.size;}function hostname(raw:string){try{return new URL(raw).hostname.toLowerCase().replace(/^www\./,'');}catch{return '';}}function blocked(raw:string){const h=hostname(raw);return BLOCKED.some(d=>h===d||h.endsWith('.'+d));}
function scoreCandidate(location:any,item:any):SiteCandidate|null{const url=String(item?.link||'');if(!url||blocked(url))return null;const title=String(item?.title||''),snippet=String(item?.snippet||''),hay=tokens(`${title} ${snippet} ${hostname(url).replace(/\./g,' ')}`),name=tokens(location.name),city=tokens(location.city),address=tokens(location.streetAddress),region=tokens(location.region);let score=0;const reasons:string[]=[];const nameHit=overlap(name,hay);if(nameHit>=.7){score+=45;reasons.push('strong business-name match');}else if(nameHit>=.4){score+=25;reasons.push('partial business-name match');}if(overlap(city,hay)>.5){score+=18;reasons.push('city match');}if(overlap(region,hay)>.5){score+=8;reasons.push('region match');}if(overlap(address,hay)>=.35){score+=22;reasons.push('address match');}if(overlap(name,tokens(hostname(url)))>=.5){score+=15;reasons.push('business name appears in domain');}score=Math.min(100,score);return{url,title,snippet,score,confidence:score>=75?'high':score>=50?'medium':'low',reasons};}
export async function discoverOfficialSite(locationId:string,actorId:string,apply=false){const location=getLocationBase(locationId);if(!location)throw new Error('Location not found.');const current=getCommunityProfile(locationId),existingUrl=String(current?.website||location.website||'').trim();if(existingUrl){const selected:SiteCandidate={url:existingUrl,title:'Existing GeoWeedo website',snippet:'',score:100,confidence:'high',reasons:['website already stored on GeoWeedo']};return{locationId,query:null,candidates:[] as SiteCandidate[],selected,applied:false,existing:true};}const key=process.env.GOOGLE_CUSTOM_SEARCH_API_KEY||'',cx=process.env.GOOGLE_CUSTOM_SEARCH_CX||'';if(!key||!cx)throw new Error('Official-site discovery is not configured. Set GOOGLE_CUSTOM_SEARCH_API_KEY and GOOGLE_CUSTOM_SEARCH_CX.');const query=[`\"${location.name}\"`,location.streetAddress,location.city,location.region,'dispensary cannabis'].filter(Boolean).join(' '),endpoint=new URL('https://www.googleapis.com/customsearch/v1');endpoint.searchParams.set('key',key);endpoint.searchParams.set('cx',cx);endpoint.searchParams.set('q',query);endpoint.searchParams.set('num','10');const response=await fetch(endpoint,{cache:'no-store'});if(!response.ok)throw new Error(`Website discovery search returned ${response.status}.`);const data=await response.json();const candidates:SiteCandidate[]=(Array.isArray(data?.items)?data.items:[]).map((x:any)=>scoreCandidate(location,x)).filter((x:SiteCandidate|null):x is SiteCandidate=>x!==null).sort((a:SiteCandidate,b:SiteCandidate)=>b.score-a.score),selected=candidates[0]||null,canApply=Boolean(selected&&selected.score>=75);const db=getDatabase();db.exec(`CREATE TABLE IF NOT EXISTS dispensary_site_discovery_runs(id INTEGER PRIMARY KEY AUTOINCREMENT,location_id TEXT NOT NULL,query TEXT NOT NULL,result_json TEXT NOT NULL,status TEXT NOT NULL,created_at TEXT NOT NULL);`);const result={locationId,query,candidates,selected,applied:Boolean(apply&&canApply),existing:false};db.prepare(`INSERT INTO dispensary_site_discovery_runs(location_id,query,result_json,status,created_at) VALUES(?,?,?,?,?)`).run(locationId,query,JSON.stringify(result),apply&&canApply?'applied':'preview',new Date().toISOString());if(apply&&canApply&&selected)upsertCommunityProfile(locationId,{overview:current?.overview,phone:current?.phone,website:selected.url,hours:current?.hours||{},amenities:current?.amenities||[],social:current?.social||{}},{type:'admin',id:actorId});return result;}
