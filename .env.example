# GeoWeedo v0.2 requires no Google Maps API key and no Google billing account.
# MapLibre/OpenFreeMap powers the guessing map and KartaView powers street imagery.
# Add future server-side secrets here only when an optional provider requires them.
