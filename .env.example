# Google Maps JavaScript API key used by the browser for Street View and the guessing map.
# Restrict this key in Google Cloud to your GeoWeedo hostname(s), for example:
# https://geoweedo.yerbas.org/*
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
